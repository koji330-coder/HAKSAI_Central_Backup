# HAKSAI Central — DESIGN.md

> **このファイルは HAKSAI Central の視覚言語（デザイン）の唯一の正（source of truth）です。**
> UI を新規作成・改修するとき、人も AI エージェント（Antigravity / Codex / Claude Code）も、
> **着手前に必ず本書を読み、既存トークンと原則に従うこと。** 色や余白の値をコードにベタ書きせず、既存の CSS 変数名を参照する。
>
> - version: **v1.4**（2026-07-12）
> - 更新方針: **doc-first**（本書を更新してからコードを直す）

---

## ⚑ 最重要の大原則：アクセントの法則（Accent Law）

**HAKSAI Central の基幹アクセントは「ゴールド」`--accent: #e8d5a3` ただ一つ。**
プロダクト全体（リサーチ／ページ案／販売中／卒業商品／発注管理／売上／診断／比較／広告）はすべてゴールド・オン・インク。

**ピンクは「秘書タブの朝礼カード（マイカ）」だけに許された例外の差し色。**
これは意図的な“ちょっとした可愛さ”のための局所例外であり、**他のタブ・コンポーネントへ波及させてはならない。**

> 🚫 エージェントへ：**ゴールドをピンクに置き換えない。** ピンク（`--sec-*`）は §2.2 のスコープ内でのみ使う。
> 「アクセント＝ピンク」と解釈するのは誤り。既定のアクセントは常にゴールド。

---

## 0. このドキュメントの使い方

- 迷ったら本書。ここに無い見た目判断が必要なら、**まず本書に追記してから**実装する。
- トークンは §2 の**既存 CSS 変数**を使う。新しい変数名を勝手に作らない（二重体系を生むため）。
- 逸脱が必要なときは理由を一行、PR / コミットに残す。変更は §7 に追記し version を上げる。

---

## 1. デザイン原則（Principles）

**基本アイデンティティ：ダーク × アイボリー × ゴールドインク（＋シグナル配色）。** 静かで精密な「標本カタログ／図鑑」の質感。

1. **アクセントはゴールド一本（Accent Law）。** ピンクは秘書の朝礼だけの例外（冒頭 ⚑ 参照）。
2. **明るさで浮かせない。** ダークで「手前」は *一段明るいダーク* で表す。白／クリームの面に飛ばすと、ページの一部でなく**別レイヤーの異物**に見える。
3. **温度と発光で語らせる。** 温かみは背景を明るくして出さず、アクセントの発光・アバター・わずかな色温度で出す。
4. **階層は「明度の梯子」。** `--bg < --surface < --surface2 < --surface3` の順に手前へ。逆に**データ・数値は暗く沈める**（`rgba(0,0,0,0.18)` 系）。
5. **アクセントは発光専用。本文色にしない。** ゴールド／ピンクは見出し・アクセントバー・CTA だけ。本文は必ず `--text` / `--muted2`。
6. **大胆さは一箇所（restraint）。** 1画面の塗りボタン（primary）は原則1つ。他は ghost / outline。
7. **コピーも素材、一要素一役割。** 同じ挨拶・ラベルを二度出さない。
8. **シグナル配色は意味に固定。** go/check/wait/stop は判断状態の意味であり、装飾に流用しない。

---

## 2. デザイントークン（Tokens）

### 2.1 コア（全ページ共通・既存の実物） ← これが正

**この変数名をそのまま使う。新規に別名を作らない。**

```css
:root {
  /* Surfaces：明度の梯子 */
  --bg:       #0c0c0e; /* 全画面の地（いちばん奥） */
  --surface:  #141417; /* カード地 */
  --surface2: #1c1c21; /* 一段手前 / 入れ子面 / スタットカード */
  --surface3: #242429; /* さらに手前 */
  --border:   rgba(255,255,255,0.07);

  /* Text */
  --text:   #eeede6; /* アイボリーの本文・見出し */
  --muted:  #6b6a65; /* 弱いラベル */
  --muted2: #9a9890; /* 補助本文 */

  /* ★ 基幹アクセント = ゴールド（唯一のブランドアクセント） */
  --accent: #e8d5a3;
  /* primaryボタン: background:var(--accent); color:#0c0c0e; active:#d4c090 */

  /* シグナル配色（判断状態＝意味に固定。装飾流用禁止） */
  --go:   #1a9e6e; --go-bg:   rgba(26,158,110,0.12); --go-bdr:   rgba(26,158,110,0.25);
  --check:#c8882a; --check-bg:rgba(200,136,42,0.12); --check-bdr:rgba(200,136,42,0.25);
  --wait: #5a8fbb; --wait-bg: rgba(90,143,187,0.12); --wait-bdr: rgba(90,143,187,0.25);
  --stop: #c04848; --stop-bg: rgba(192,72,72,0.12);  --stop-bdr: rgba(192,72,72,0.25);
  --blue: #4a8fd4; --blue-bg: rgba(74,143,212,0.12);

  /* 書体 */
  --disp: 'Syne', sans-serif;       /* ディスプレイ（ワードマーク等） */
  --sans: 'Noto Sans JP', sans-serif;/* 本文 */
  --mono: 'DM Mono', monospace;      /* 数値・データ */
}
```

### 2.2 秘書スコープ限定（マイカ朝礼カード）★ここだけの例外

> 🚫 **このブロックのトークンは「秘書タブの朝礼カード」以外で使用禁止。**
> `--sec-` 接頭辞は「秘書スコープ専用」の目印。他コンポーネントに付けない。
> 本文・数値・書体は**コア（§2.1）を流用**して系統に繋ぎ止め、差し色だけピンクにする。

```css
:root {
  /* 朝礼カードの器（コアより一段明るい＆ほのかにマゼンタ寄り＝主役） */
  --sec-brief-bg:     #221f2a;
  --sec-brief-bdr:    rgba(247,168,205,0.16);
  --sec-brief-glow:   radial-gradient(120% 90% at 0% 0%, rgba(236,72,153,0.10), transparent 60%);
  --sec-brief-shadow: 0 8px 32px rgba(0,0,0,0.35), 0 0 48px rgba(236,72,153,0.06);

  /* 秘書の差し色ピンク（発光専用） */
  --sec-pink:      #ec4899; /* CTA塗り・左アクセントバー */
  --sec-pink-hover:#f472b6;
  --sec-pink-soft: #f7a8cd; /* 暗所で読める見出しピンク */
  --sec-pink-bdr:  rgba(236,72,153,0.35);

  /* 今日の一手コールアウト */
  --sec-callout-bg: #241a22;
}
```

---

## 3. コンポーネント規約（Components）

### 3.1 サーフェス / エレベーション
- カード：`--surface`（標準）。入れ子や一段手前は `--surface2` / `--surface3`。角丸は 12〜16px。境界は `--border`。
- **データ・数値の面は沈める**：`background: rgba(0,0,0,0.18)`（既存 diag-kpi と同じ沈め方）。数値は `--mono`。
- 主役カードだけ影＋わずかな発光を許可。乱用しない（主役は基本1つ）。

### 3.2 ボタン（序列を必ず作る）
| 種別 | 用途 | 見た目 |
|---|---|---|
| **primary** | その画面の主行動（1つだけ） | `background:var(--accent)`（ゴールド）/ 文字 `#0c0c0e` |
| **ghost** | 副次アクション | 透過 / `--border` 強め / 文字 `--muted2`（hover で `--text`） |
| **danger/stop** | 破壊・停止系 | `--stop` 系 |

> 秘書の朝礼カード内の primary（`読んだ`）だけは例外的に `--sec-pink` 塗りを許可（可愛さの担保）。系統に寄せたい場合はゴールド `--accent` でも可。

### 3.3 シグナルバッジ / 判断チップ
- GO=`--go`、要確認=`--check`、様子見=`--wait`、見送り=`--stop`、情報=`--blue`。それぞれ `-bg` 地＋`-bdr` 縁。
- **色＝判断の意味**。見た目を可愛くする目的で色を変えない。

### 3.4 見出し・アクセントテキスト
- 発光見出しは `--accent`（ゴールド）。秘書朝礼カード内のみ `--sec-pink-soft`。
- 本文・ラベルにアクセント色を使わない。

### 3.5 コールアウト
- 左 `3px solid`（ゴールド or 秘書内は `--sec-pink`）＋ 全周境界 ＋ 地（`--surface2` or `--sec-callout-bg`）。
- **単辺ボーダーだけのとき角丸を付けない**（全周境界を併用するときのみ角丸可）。

---

## 4. リファレンス実装

### 4.1 標準カード（ゴールド系・全ページの手本）
リサーチ／診断／比較などのカード。地 `--surface`、境界 `--border`、見出し `--text`、サブ・注記 `--muted2`、数値 `--mono`、強調・リンクは `--accent`。判断状態は §3.3 のシグナル。**ここにピンクは登場しない。**

### 4.2 朝礼カード（秘書スコープの例外・可愛さ担保）
秘書タブのヒーロー。原則2〜7が全部乗った基準実装。差し色だけ §2.2 のピンク、本文・書体はコアを流用。

**構造（上から）**
1. **ヘッダー行**：アバター（40px 円 ＋ `--sec-pink-bdr` リング、中はマイカのアイコン／立ち絵）＋ 見出し（`--sec-pink-soft`、絵文字・❤️ 等マイカの声はそのまま）
2. **件数サブ行**：「今日は N 件、気になることがあります」を `--muted2` / 小さめで（→ §4.3 で一元化）
3. **本文**：`--text`（アイボリー）/ line-height 1.7
4. **実績帯（沈み面）**：`rgba(0,0,0,0.18)` に「昨日の実績」を沈めて表示、数値は `--mono`
5. **今日の一手（コールアウト）**：地 `--sec-callout-bg`、左 `3px solid var(--sec-pink)`、全周 `--sec-pink-bdr`。見出し語「今日の一手：」を `--sec-pink-soft`
6. **アクション行**：`読んだ`=primary（`--sec-pink` 塗り）／`設定`・`全体相談`・`PDF診断`=ghost／`状況を更新`=`--sec-pink-bdr` のアウトライン

**カード地**：`--sec-brief-bg` ＋ `--sec-brief-bdr` ＋ `box-shadow: var(--sec-brief-shadow)` ＋（左上に `--sec-brief-glow`）

### 4.3 ★挨拶はカードに一元化（恒久ルール）
> 秘書ページ上部の**ゴールドの挨拶（sec-hero / sec-greeting「こんにちは、ガッキー。／マイカです。今日は N 件…」）を削除**し、
> 挨拶・件数の告知は**朝礼カード内に集約**する。同じ挨拶を2箇所に出さない（原則7）。
> 実装手順は指示書 `briefing_card_redesign_request.md` を参照。

### 4.4 ページ制作の進捗とマイカ発話カード（コアトークン版）
- ページ制作では「完了・現在地・要確認・ロック・未着手」を5段階ステッパーで示す。完了=`--go`、要確認=`--check`、現在地=`--accent`、ロック／未着手=`--muted` を使う。
- 現在地に対応する主行動を1つだけゴールドの primary とし、その他の操作は ghost / outline にする。
- ページ制作内のマイカ発話カードは朝礼カードではないため、器・境界・名前ラベル・アバターリングをすべてコアトークンで構成し、`--sec-*` を使用しない。
- マイカの同一性は朝礼画面と同じアバター素材、名前、口調で示す。汎用の人物絵文字をアバター代わりにしない。
- 発話は既存の案件状態から即時生成し、画面表示のためにAIや追加APIを呼ばない。
- ロックされた操作は無反応な disabled にせず、理由・不足項目・解除する操作を表示する。

### 4.5 ページ制作一覧の進捗グループと寝かせ行
- 一覧は日付の平坦な並びではなく、同一の労力判定から「おかえり／あと1タップ／AI／仕上げ／素材待ち」に分類する。
- ゴールド見出しとゴールド塗りは「あと1タップ」の主行動だけに使う。他グループは `--muted2`、期限到来・停滞は判断状態として `--check` を使う。
- カードの5ドットは詳細画面と同じ進捗状態を縮約して表す。別の状態体系を作らない。
- 寝かせ中は `--muted` 系の畳み行へ集約するが、件数は常に表示し、完全に隠さない。
- 停滞警告は必ず「進める／寝かせる／撤退」の操作と一体にし、警告だけを置かない。

### 4.6 卒業商品の仮説検証キュー
- 卒業商品一覧は「答え合わせ待ち／観測中／検証済み」の順にグループ化し、平坦な商品一覧にしない。
- ゴールド見出しは「答え合わせ待ち」に限定する。観測中は `--wait`、的中は `--go`、部分的中は `--check`、外れは `--stop` を判断の意味として使う。
- 売価・月販・利益率の対比は `rgba(0,0,0,0.18)` の沈み面に置き、数値は `--mono` を使う。
- 一覧表示と詳細表示ではAIや追加APIを呼ばず、初期ロード済みデータだけから状態を導出する。
- 振り返り生成を画面の主行動とし、ASIN更新などは ghost / outline にする。横展開の種は参考表示に留め、カード生成CTAを置かない。
- 一覧末尾の教訓ライブラリは、ページ制作の寝かせ行と同様に件数を常時表示する折りたたみ行とする。展開時はverdict、再利用可能な教訓、元商品の実例リンクを表示する。

---

## 5. アンチパターン（やってはいけない）

- ❌ **ゴールドをピンクに置き換える／ピンクを秘書タブの外へ波及させる**（最重要禁止・Accent Law 違反）
- ❌ **勝手に別名トークンを作る**（`--bg-page` 等）→ 既存 `--bg` / `--accent` … を使う
- ❌ 白／明るい面で要素を浮かせる（明度の反転）
- ❌ アクセント色（ゴールド／ピンク）を本文色に使う
- ❌ シグナル配色を装飾目的で流用する（色＝判断の意味を壊す）
- ❌ 同じ挨拶・ラベルを2箇所に出す
- ❌ データ・数値の面を周囲より明るくする（沈ませる約束を破る）
- ❌ 単辺ボーダー（`border-left` だけ等）に角丸を付ける
- ❌ 1画面に塗りボタン（primary）を複数置く

---

## 6. 運用フロー

1. **doc-first**：見た目を変える前に本書を更新 → その後コード。
2. **エージェント運用**：Antigravity / Codex / Claude Code は UI 着手前に本書を読む（`.agents` から参照させる。§配置参照）。**特に Accent Law を最初に確認する。**
3. **逸脱記録**：やむを得ず外すときは理由を一行残す。
4. **version 更新**：変更したら §7 に追記し、冒頭の version を上げる。

---

## 7. 変更履歴（Changelog）

- **v1.4**（2026-07-12）卒業商品の仮説検証キュー、verdict配色、対比表、表示時の追加呼び出し禁止を追加。
- **v1.3**（2026-07-11）ページ制作一覧の労力別グループ、ミニ進捗、停滞再判断、寝かせ行の表示規約を追加。
- **v1.2**（2026-07-11）ページ制作の5段階ステッパー、主行動の一元化、コア配色のマイカ発話カード、説明可能なロック表示を追加。
- **v1.1**（2026-07-11）**Accent Law を新設**：基幹アクセントをゴールド `--accent #e8d5a3` に確定し、ピンクを秘書朝礼スコープ（`--sec-*`）の例外へ隔離。コアトークンを既存 `:root` の実物に修正（自作変数名を廃止）。アンチパターン筆頭に「ゴールド→ピンク置換／ピンク波及」を追加。
- **v1.0**（2026-07-11）初版。※ピンクを基幹アクセントとして誤記していたため v1.1 で是正。

---

## 配置（このファイルの置き場所）

- **本ファイルはプロジェクトルート直下**（`.clasp.json` / `appsscript.json` と同階層）に置く。ルートは各 AI エージェントが最初に走査する場所。
- **`.claspignore` に `DESIGN.md` を追記**（GAS へ push 不要）。指示書 `briefing_card_redesign_request.md` も同様。
- **`.agents` にポインタを1行**：「UI・デザイン作業の着手前に `/DESIGN.md`（特に Accent Law）を読む」。
- UI を `_github_HAKSAI-Central/` から配信している場合も**正はルートの本書1つ**。当該フォルダには複製せず `See ../DESIGN.md` の参照行だけ置く（二重管理禁止）。
