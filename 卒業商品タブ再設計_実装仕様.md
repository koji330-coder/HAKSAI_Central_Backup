# HAKSAI Central「卒業商品」タブ再設計 実装仕様

- 元設計: `C:\Users\koji3\Downloads\graduated_tab_redesign_spec.md`
- 準拠: `DESIGN.md` v1.4
- 現行コード監査反映日: 2026-07-12

## 1. 目的と卒業条件

卒業商品タブは、リサーチ仮説を販売実績で答え合わせし、教訓を次のリサーチへ還流する画面とする。

卒業条件は次のすべてを満たすこと。

1. `is_deleted !== true`
2. `origin_type === 'research'`
3. `is_launched === true`
4. lifecycleカードまたは紐づくページ制作案件の `own_listing` に、自社ASINが1件以上ある

競合ASINを格納しうる `card.asin`、および `connected_at` の有無だけでは卒業判定しない。

## 2. 現行コード監査で補正した事項

### 2.1 JSON復元

`own_listing` は `getAllCards_ProductLifecycle()`、`getCardDetail_ProductLifecycle()`、ページ案同期で必ずJSONオブジェクトとして復元する。個別 `jsonCols` の列挙漏れを禁止し、可能な箇所では共通定数を使う。

### 2.2 origin_type

`product_lifecycle` に `origin_type` を追加する。

- `research`: 手動分析、Seller ATLAS、ATLAS ASIN research、ATLAS sourcing decision、秘書の競合分析、卒業商品から作る種カード
- `import`: `bulkRegisterSellingProducts()` による在庫インポート

保存関数では未設定値を安全側に補うが、直接行を書き込む経路にも明示設定する。ATLASのUPSERT更新では既存値を保持する。

backfillは次の2段階にする。

1. `auditOriginTypeBackfill()` が source別件数、推定分類、own_listing混入候補を読み取り専用で返す
2. `backfillOriginType()` が空欄の `origin_type` 列だけを更新する

旧データの `inventory_import`、`csv_import`、`merged_duplicate`、source空欄は `import`、それ以外を `research` とする。既設定値は上書きしない。重複統合済みの正本は統合処理が明示的に `research` を設定する。

### 2.3 own_listing接続

通常のカード保存では `own_listing` を生成・変更しない。自社ASINは `connectOwnListing` またはページ案の `saveOwnListingLink` からのみ変更する。

両経路で以下をサーバー検証する。

- ASIN形式を大文字10桁へ正規化
- 1件以上必須
- `sku_master` に存在すること
- `research_lineage.competitor_asins` に含まれないこと

接続イベントでは `is_launched=true` とし、代表ASINを `card.asin` に同期する。`connected_at` は初回値を保持する。

### 2.4 ページ案同期

`updateCardInLifecycle()` は汎用更新として `document_status` を変更しない。保存前のシート値と正規化した `own_listing` のJSONを比較し、実際に変化した場合だけページ案へ同期する。

接続専用イベントだけが、ページ案の `document_status` が空欄の場合に `candidate_selected` を設定する。既存値は上書きしない。

ページ案とlifecycleの片方だけが更新される状態を避けるため、接続処理はScriptLock内で実行し、同期失敗を握り潰さず呼び出し元へ返す。

## 3. 仮説検証データ契約

### 3.1 販売開始日

優先順は次のとおり。

1. 妥当な `own_listing.connected_at`
2. `amazon_published_date`
3. `sales_actual` の最古 `period` の月初

過去の通常保存で生成された可能性がある `connected_at` は監査対象に含める。日付として解釈できない値は無視する。

### 3.2 直近3ヶ月実績

`sales_actual` を `period` 昇順へ正規化し、最新3期間を使う。

- 実績売価: `sum(sales_taxin) / sum(qty)`
- 月販: 対象期間の `qty` 平均
- 粗利益率: 原価が揃った期間の `sum(gross_profit) / sum(sales_taxin)`
- 原価不足または数量0: データ待ち

仮説値が正の数である場合のみ、`abs(actual-hypothesis)/abs(hypothesis) <= 0.15` を的中とする。

### 3.3 ステータス

- `verified`: 最新のresearch lessonがある
- `due`: lessonがなく販売開始から90日以上
- `observing`: それ以外

lessonは `created_at` 昇順で並べ、末尾を最新として扱う。

## 4. research_lessons

元設計の列を持つ追記専用シートとする。書き込みは `saveResearchLesson_()` に集約し、既存行を更新・削除しない。

初期ロード用にGET `getResearchLessons` を追加する。一覧描画時にはAPIを呼ばず、コアデータ初期化時に一度取得したキャッシュを使う。

`generateRetrospective` はシート正本から仮説・実績を再構築する。HTTP再試行とは別に、JSON解析またはスキーマ検証失敗時だけ1回再生成する。次を検証してから保存する。

- verdictが `的中 / 部分的中 / 外れ` のいずれか
- 4つの出力項目が配列
- lessonsの `applies_to` が `sourcing / ad / listing` のいずれか
- 不正な結果は保存しない

## 5. 横展開と類似教訓

- `next_seeds` は具体的な商品名で表現できる参考アイデアとして表示だけ行い、カード生成導線を持たない
- `source='graduated_seed'` の新規生成経路は廃止する
- POST `getLessonsForCandidate`: カテゴリ、価格帯、テーゼタグで決定論的に上位5件を返す
- 返却する各教訓には、元カード、仮説値、実績値、販売日数を含む `example` を必須とする
- 卒業タブ最下部に、初期ロード済みキャッシュだけで描画する折りたたみ式の教訓ライブラリを置く

## 6. UI規約

- 答え合わせ待ち、観測中、検証済みの順にグループ表示
- ゴールドは答え合わせ待ち見出しと画面の主行動1つに限定
- 的中=`--go`、部分的中・要確認=`--check`、外れ=`--stop`、観測中=`--wait`、不足=`--muted`
- 数値面は `rgba(0,0,0,0.18)`、数値は `--mono`
- 一覧・詳細表示だけでAIや追加APIを呼ばない

## 7. 受け入れテスト

元設計の12条件に加え、次を必須とする。

1. 一覧・詳細APIで `own_listing` がオブジェクトとして返る
2. ATLAS UPSERTで既存 `origin_type` が消えない
3. 通常保存でページ案のown_listingとdocument_statusが変わらない
4. lessonsの取得順に依存せず最新行が選ばれる
5. `sales_actual` が未ソート、3ヶ月未満、数量0、原価不足でも例外にならない
6. 接続同期失敗が成功レスポンスとして返らない
7. JSON形式は正しいがスキーマ不正なGemini結果を保存しない

## 8. 実装順

1. 本仕様と `DESIGN.md` 更新
2. JSON復元、origin_type、監査、backfill、卒業判定
3. 接続専用APIと同期副作用修正
4. 仮説・実績パックとグループUI
5. research_lessonsと振り返り生成
6. 実例付き類似教訓APIと教訓ライブラリ
7. 専用テスト、既存回帰テスト、Apps Script構文確認
